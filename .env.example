DATABASE_URL=
# Must be at least 32 characters long.
NEXTAUTH_SECRET=
# Must be an absolute HTTP(S) URL, such as http://localhost:3000.
NEXTAUTH_URL=
# Optional testing-only group. Configure all three entries together with
# synthetic or redacted values only; it is not authorized for Production.
# Uncomment all three entries together to enable inbound-email testing.
# INBOUND_EMAIL_API_KEY=
# INBOUND_EMAIL_WEBHOOK_SECRET=
# Hostname only (no scheme, path, userinfo, or port).
# INBOUND_EMAIL_DOMAIN=
